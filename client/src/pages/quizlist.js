import React from "react";
import  NavTemplate  from "../components/template/NavBar"
const Quizlist = () => {
    
    return(
        <div>
           <NavTemplate/>
        </div>
    );
}

export default Quizlist;